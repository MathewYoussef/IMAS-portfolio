# Claims Ledger Overview

Each claim in the manuscript should link to executable evidence: notebook cell, processed dataset (with checksum), code path, and a simple test.

## Structure

- Claim pages live under `docs/claims/C-XXX.md`.
- A generated ledger (to be produced by Agent 3) will summarize coverage.

## Authoring Notes

- Use `thesis.docx.md` as the excerpt source while drafting.
- Cite line ranges for auditability.
